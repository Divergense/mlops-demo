from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='google store app rating prediction',
    author='raf',
    license='MIT',
)
